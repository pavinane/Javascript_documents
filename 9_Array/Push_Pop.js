let arr = [1, 2, 3, 4, 5, 6, 7, 8, 9];

// push method is used to add the value in the end of the array
arr.push(10);

console.log(arr);

// pop method is used to remove the value from the end of the array

let popActivity = ["work", "eat", "sleep"];

console.log("remove item", popActivity.pop()); //show the whichone is removed
console.log("pop", popActivity);
